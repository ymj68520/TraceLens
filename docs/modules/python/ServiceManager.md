# ServiceManager（服务生命周期协调者）— 已迁移

本文件是重定向页。ServiceManager（`python_service/httpserver/services/service_manager.py`）的规范文档已迁移至
[httpserver/services/ServiceManager.md](./httpserver/services/ServiceManager.md)。

迁移原因：根目录下的旧文档与 `httpserver/services/` 子目录下的文档内容重复，且已过时（缺少启动分层超时、
初始化回滚、生命周期状态机等现行机制）。保留一份规范文档以便维护。

请更新你的书签/链接：**[httpserver/services/ServiceManager.md](./httpserver/services/ServiceManager.md)**

**最后更新**: 2026-08-23（解释式重写）
