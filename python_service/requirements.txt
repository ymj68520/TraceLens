# Database
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
alembic>=1.11.0

# Authentication
pyjwt>=2.8.0
passlib[bcrypt]>=1.7.4
# bcrypt >=4.1 removed bcrypt.__about__ and changed the C extension API, which
# breaks passlib 1.7.4's bcrypt backend ("password cannot be longer than 72
# bytes" even for short inputs). Pin below 4.1 until passlib releases a fix.
bcrypt>=4.0.1,<4.1
python-multipart>=0.0.6

# Additional FastAPI dependencies
python-jose[cryptography]>=3.3.0

# API validation / schemas
pydantic>=2.0.0
email-validator>=2.0.0  # required by pydantic.EmailStr

# Configuration
pydantic-settings>=2.1.0

# Web server
uvicorn>=0.23.0

# Graph analysis (WeChat relationship graph feature)
networkx>=3.0
# Optional analysis enhancements (pagerank / community detection); the code
# degrades gracefully without them.
# scipy>=1.10.0
# python-louvain>=0.16
