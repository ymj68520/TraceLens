# FastAPI HTTP Server Dependencies
# For ForensicsProject Python Service

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Async HTTP Client
httpx>=0.25.0

# Data Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0

# File Uploads
python-multipart>=0.0.6

# Environment Variables
python-dotenv>=1.0.0

# Async Utilities
anyio>=4.0.0

# Logging
structlog>=23.2.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# Graphiti knowledge graph integration
graphiti-core>=0.3.0
neo4j>=5.14.0

# Optional: OpenAI client (uncomment if using official client)
# openai>=1.3.0

# Office Document Parsing
python-pptx>=0.6.21
openpyxl>=3.1.0
xlrd>=2.0.1

# Image Processing
Pillow>=10.0.0

# Document/PDF Extraction
PyMuPDF==1.27.1
python-docx==1.2.0
plyvel>=1.5.0

pymongo>=4.6.0
rdbtools>=0.1.15
redis>=5.0.0
redis-dump-load>=1.1

# Markdown Conversion (primary file-to-markdown engine)
# Includes: PDF, DOCX, XLSX, XLS, PPTX, HTML, CSV, JSON, XML, EPUB,
#           images (EXIF+OCR), audio (transcription), and more
markitdown[all]>=0.1.6

# WeChat Graph Analysis
networkx>=3.0
python-louvain>=0.16

# Forensic Format Parsing
python-evtx>=0.8.0        # Windows Event Log (.evtx)
python-registry>=1.3.0    # Windows Registry hives
olefile>=0.47             # OLE2 format (.msg, jump lists)
defusedxml>=0.7.1         # Safe XML parsing (XXE protection)

# Image Format Support
pillow-heif>=0.13.0       # HEIC/HEIF image metadata
rawpy>=0.19.0              # RAW camera images (CR2/NEF/ARW/DNG)

# Audio/Video Metadata
mutagen>=1.47.0            # Audio file metadata (MP3, FLAC, OGG, etc.)

# Binary Analysis
pefile>=2023.2.7           # PE file analysis (EXE/DLL)
pyelftools>=0.30           # ELF file analysis (SO/LD/KO) and GRUB bootloader modules (.mod)

# Document Formats
striprtf>=0.0.26           # RTF document parsing
odfpy>=1.4.1               # ODF documents (ODT/ODS/ODP)

# Calendar and Contacts
icalendar>=5.0.0           # ICS calendar files
vobject>=0.9.6             # VCF contact files

# Font Analysis
fonttools>=4.40.0          # Font metadata (TTF/OTF/WOFF)

# Disk Images
pycdlib>=1.14.0            # ISO disc image parsing

# Compression
lz4>=4.3.0                 # LZ4 compression
zstandard>=0.22.0          # Zstandard compression

# TOML parsing (for Python < 3.11)
tomli>=2.0.0; python_version < "3.11"

# Scientific data formats
h5py>=3.10.0               # HDF5 files
pydicom>=2.4.0             # DICOM medical imaging
